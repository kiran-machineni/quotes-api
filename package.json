{
  "name": "quote-api",
  "version": "1.0.0",
  "description": "A small quote-serving JSON API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --inspect server.js",
    "test": "jest",
    "prestart": "if [ -z \"$PORT\" ]; then echo 'Error: PORT environment variable is not set.'; exit 1; fi"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
